version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    networks:
      - appnet

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    ports:
      - "80:80"
    networks:
      - appnet

networks:
  appnet:
    driver: bridge
